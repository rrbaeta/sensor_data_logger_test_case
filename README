Setting Up:

Install PlatformIO IDE extension for Visual Studio Code

//==============================================================================

pytest instructions:

Generate so files to use with pytest:
gcc -shared -o temperature_sensor.so -fPIC src/sensor.c src/circular_buffer.c

To run tests do "pytest" in the terminal